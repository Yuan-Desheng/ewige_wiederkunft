Dev server 正常启动，无编译错误。来看下完成的改动总结。

---

## 完成总结

### 新建文件 (3 个)

1. **`docs/api/load-forecast-api.md`** — 完整 API 文档，含请求参数、响应结构、KPI 映射关系
2. **`src/api/forecast.ts`** — API 层，定义 `CoreLoadData` 类型 + `getLoadCoreData()` 函数
3. **`src/pages/forecast/composables/useKpiApi.ts`** — composable，封装数据请求 + `buildKpiItems()` 转换函数

### 修改文件 (3 个)

4. **`src/pages/forecast/components/KpiSectionRows.vue`** — 核心改动：
   - 新增 props: `city`, `district`, `userNo`（可选过滤参数）
   - 用 `useKpiApi` composable 替换硬编码数据
   - 6 个 KPI 从 API 获取（实时负荷、最高负荷、峰谷差、累计用电、峰值负荷、接入用户）
   - 3 个无接口的 KPI 保留为 `--` 占位（近三年均值、用电趋势、预测误差）
   - API 异常时自动降级到 `--` fallback

5. **`src/pages/forecast/city/[name].vue`** — 传递 `:city="cityName"` 给 KpiSectionRows
6. **`src/pages/forecast/district/[name].vue`** — 传递 `:city="cityName" :district="districtName"`

### API 路由链路

```
浏览器 → /api/data/api/load-forecast/core-data-24h
  → Vite/Nginx 代理去掉 /api 前缀
    → pig-gateway:9999/data/api/load-forecast/core-data-24h
      → pig-data-biz 服务处理
```

已验证 dev (port 28200) 和 gateway (port 9999) 两条路径均返回 `code: 0`。当前 API 返回测试数据（大部分为 0），组件会正确显示格式化后的值。