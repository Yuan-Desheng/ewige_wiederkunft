---
fileName: 周计划模块
tags:
  - 模块
  - 周末计划
multiFile: 
contact:
  - 周计划: 周读书计划（h）
    目标值: 7
    周计划目标值: 6
  - 周计划: 周喝水量达标（次）
    目标值: 6
    周计划目标值: 6
  - 周计划: 周运动计划（h）
    目标值: 7
    周计划目标值: 6
  - 周计划: 周复盘（次）
    目标值: 6
    周计划目标值: 6
  - 周计划: 周回顾建立联系（篇）
    周计划目标值: 60
---

```js-engine
const mb = engine.getPlugin('obsidian-meta-bind-plugin').api;

const tableOptions = {
	bindTarget: mb.createBindTarget('frontmatter', context.file.path, ['contact']),
	tableHead: ['周计划', '目标值', '完成值'],
	columns: [
		'INPUT[text(class(text-100)):scope^周计划]',
		'INPUT[number(class(text-50)):scope^周计划目标值]',
		'INPUT[number(class(text-50)):scope^周计划完成值]',
	],
};

const mountable = mb.createTableMountable(context.file.path, tableOptions);

mb.wrapInMDRC(mountable, container, component);

```
